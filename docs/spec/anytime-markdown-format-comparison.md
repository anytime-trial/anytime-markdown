# anytime-markdown 対応フォーマット比較

更新日: 2026-03-06

GFM 仕様と anytime-markdown エディタの対応状況を比較する。

---

## GFM 仕様との対応

| GFM 構文 | エディタ対応 | Markdown 出力 | 備考 |
| --- | --- | --- | --- |
| ATX 見出し (`#`) | 対応（レベル1-5） | `# ` ～ `##### ` | レベル6は未対応 |
| フェンスコードブロック (`` ` ``) | 対応 | `` ``` `` | チルダフェンス (`~`) は非対応 |
| テーブル | 対応 | `\| --- \|` 形式 | セパレータは `\| --- \|` に正規化 |
| コードスパン | 対応 | `` ` `` ～ | テーブル行内はバッククォート最小化 |
| 箇条書きリスト (`-`) | 対応 | `- ` | `+` `*` は非対応 |
| 順序付きリスト (`1.`) | 対応 | `1. ` | `)` 形式は非対応 |
| タスクリスト | 対応 | `- [ ]` `- [x]` | ネスト対応 |
| 太字 (`**`) | 対応 | `**text**` | `__` は非対応 |
| 斜体 (`*`) | 対応 | `*text*` | `_` は非対応 |
| 取り消し線 (`~~`) | 対応（StarterKit） | `~~text~~` | |
| リンク | 対応 | `[text](url)` | 参照リンクは非対応 |
| 画像 | 対応 | `![alt](url)` | Base64 埋め込み対応 |
| 水平線 | 対応（StarterKit） | `---` | |
| ハードブレーク | 対応 | 行末 `\` | |
| ブロック引用 | 対応 | `> ` | Admonition 拡張あり |
| バックスラッシュエスケープ | 対応（GFM 準拠） | | テーブルセル内は後処理で復元 |
| エンティティ参照 | 対応（GFM 準拠） | | テーブルセル内は後処理でデコード |

## GFM 仕様外の独自拡張

| 構文 | エディタ対応 | Markdown 出力 | 備考 |
| --- | --- | --- | --- |
| ハイライト | 対応 | `<mark>text</mark>` | HTML タグで出力 |
| 下線 | 対応 | `<u>text</u>` | HTML タグで出力 |
| 数式（インライン） | 対応 | `$expr$` | MathInline 拡張 |
| 数式（ブロック） | 対応 | `$$\n...\n$$` | コードブロック `math` から変換 |
| Mermaid 図 | 対応 | `` ```mermaid `` | コードブロック言語指定 |
| PlantUML 図 | 対応 | `` ```plantuml `` | コードブロック言語指定 |
| 折りたたみ | 対応 | `<details><summary>` | HTML タグで出力 |
| 脚注 | 対応 | `[^1]` / `[^1]: ` | FootnoteRef 拡張 |
| Admonition | 対応 | `> [!NOTE]` 等 | ブロック引用を拡張 |
| コメント | 対応 | `<!-- comment-start:id -->` | 入力時に `<span>` に変換 |
| 下付き文字 | HTML のみ | `<sub>text</sub>` | 専用拡張なし |
| 上付き文字 | HTML のみ | `<sup>text</sup>` | 専用拡張なし |
