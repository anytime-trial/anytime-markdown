# セキュリティポリシー

[日本語](SECURITY.ja.md) | [English](SECURITY.md)

## 脆弱性の報告

**セキュリティ上の問題を公開 issue に書かないでください。**

GitHub の非公開報告を使ってください: [**脆弱性を報告する**](https://github.com/anytime-trial/anytime-markdown/security/advisories/new)。本リポジトリでは private vulnerability reporting が有効になっているため、修正が公開されるまで報告内容は報告者とメンテナだけに見えます。

報告には次を含めてください。

- 該当コンポーネント — VS Code 拡張名 / MCP サーバー / 共有パッケージ / Web アプリ
- バージョン — 拡張なら Marketplace のバージョン、ソースからの利用ならコミットハッシュ
- 再現手順と、攻撃者が何を得られるか
- 実行環境 — OS ・ VS Code バージョン ・ Node.js バージョン

本プロジェクトは業務時間外に少人数で保守しています。初回の受領連絡までおよそ 1 週間を見込んでください。報奨金制度はありません。

## サポート対象バージョン

セキュリティ修正が入るのは各コンポーネントの最新公開版のみです。長期サポートブランチはありません。

| コンポーネント | サポート |
| --- | --- |
| publisher `anytime-trial` の VS Code 拡張 | Marketplace の最新リリース |
| npm パッケージ `@anytime-markdown/markdown-view` ・ `markdown-view-lite` | 最新公開版 |
| `master` ブランチ | あり |
| `develop` ・ `feature/*` ブランチ | なし（リリースではなく開発中のブランチ） |
| 過去のリリース | なし（最新版へ更新してください） |

## 対象範囲

対象:

- publisher `anytime-trial` で公開している VS Code 拡張
- `packages/mcp-*` の MCP サーバー（パス解決やローカル DB の読み取り方を含む）
- `packages/web-app` の Web アプリと `anytime-trial.com` でのデプロイ
- 上記から利用される範囲での `packages/*` の共有パッケージ

対象外:

- 本プロジェクト経由の攻撃経路が無い、依存パッケージ自体の脆弱性 — 上流へ報告してください。ただし**本プロジェクトの使い方において**悪用可能な場合は対象であり、ぜひ知らせてください
- 攻撃者が既にユーザーのマシンやワークスペースへの書き込み権限を持っていることを前提とする指摘
- 影響を実証できない堅牢化の不足 — スキャナの検出結果のみでは報告として成立しません

## 報告後の流れ

1. 受領を連絡し、再現を試みます。
2. 公開時期を報告者と合意します。既定は 90 日で、既に公開されている問題ならより短くて構いません。
3. 修正して公開し、advisory に報告者をクレジットします（匿名を希望する場合は記載しません）。

## 資格情報とローカルデータ

いくつかのコンポーネントはマシン上の資格情報・ローカルデータを読み取ります。本プロジェクトがそれらを外部へ送信することはありません。

- `mcp-cms` は `DOTENV_CONFIG_PATH` で指定したローカル `.env` から S3 の資格情報を読む
- `read_google_doc` は `GOOGLE_SERVICE_ACCOUNT_KEY_PATH` のパスから Google サービスアカウントキーを読む。未設定ならこのツール自体が登録されない
- `mcp-trail` と Anytime Trail 拡張は、ワークスペース内 `.anytime/` 配下の SQLite データベース（AI セッション履歴を含む）を読む
- Anytime Agent 拡張はホームディレクトリの Claude Code ・ Codex のセッションファイル（`~/.claude/projects` ・ `~/.codex/sessions`）を読む

`.gitignore` は `.env*`（`*.example` を除く）・ `*service-account*.json` ・ `*.pem` を除外しており、これらが誤ってコミットへ入らないようにしています。本プロジェクトが資格情報・セッション記録・ワークスペースの内容を、あってはならない場所（ログファイル・配布物・外向きのリクエスト）へ書き出す経路を見つけた場合は、対象範囲内なので報告してください。
