---
title: "{{リポジトリ名}} 未明文のコーディング規約（doctrine）"
date: "{{YYYY-MM-DD}}"
type: "spec"
lang: "ja"
author: "Claude Code v{{CLI バージョン}}"
skill: "anytime-dev-retro"
category: "doctrine"
excerpt: "{{200 文字以内。未明文パターンの要旨}}"
extractedAt: "{{ISO 8601}}"
commitRange: "{{from}}..{{HEAD}}"
sources:
    - "{{使用した入力ソース（例: サンプリングしたパッケージ / レビュー指摘）}}"
---

# {{リポジトリ名}} 未明文のコーディング規約

## 概要

{{実コードが一貫して従うが明文規約に無いパターンの全体像を 2〜3 文で要約する。サンプリング範囲（対象パッケージ・ファイル数）を明記する}}

## パターン一覧

<!-- 状態=暗黙のパターンのみ（明文化済みは末尾の参考節へ）。記入例: -->

### エラーを握りつぶさず識別子付きでログする

- 確度: 原則
- 状態: 暗黙
- 承認: draft
- 主張: catch 節は空にせず、エラー内容と対象の識別子（ファイルパス等）を必ずログへ残す。
- 根拠:
    - 「{{逐語引用（実コード 1）}}」 — `{{出典パス}}`
    - 「{{逐語引用（実コード 2）}}」 — `{{出典パス}}`
    - 「{{逐語引用（レビュー指摘）}}」 — {{レビュー ID またはパス}}

## 明文化済み重複（参考）

<!-- サンプリングで検出したが既存明文規約と重複したパターン。重複先パスを必ず示す。暗黙知に数えない -->
