---
title: "{{リポジトリ名}} 設計哲学・製品原則（doctrine）"
date: "{{YYYY-MM-DD}}"
type: "spec"
lang: "ja"
author: "Claude Code v{{CLI バージョン}}"
skill: "anytime-reverse-doctrine"
category: "doctrine"
excerpt: "{{200 文字以内。抽出した原則の要旨}}"
extractedAt: "{{ISO 8601}}"
commitRange: "{{from}}..{{HEAD}}"
sources:
    - "{{使用した入力ソース（例: git log / spec / memory_reviews）}}"
---

# {{リポジトリ名}} 設計哲学・製品原則

## 概要

{{この製品が暗黙に優先しているものを 2〜3 文で要約する}}

## 原則

<!-- 確度=原則（独立根拠 3 件以上・反例 1 件以下）のみをこの節に置く。記入例: -->

### 安定性・既存挙動を最適化より優先する

- 確度: 原則
- 状態: 暗黙
- 承認: draft
- 主張: 修正・変更の選択で、既存挙動の維持が性能・構造の最適化より優先される。
- 根拠:
    - 「既存テスト・動作・周辺機能への影響を最小化」 — `{{出典パス}}`
    - 「{{逐語引用 2}}」 — {{コミットハッシュ}}
    - 「{{逐語引用 3}}」 — `{{出典パス}}`

## 傾向

<!-- 確度=傾向（反例 2 件以上で原則から格下げ）の主張。格下げ根拠の反例も併記する -->

## 仮説

<!-- 確度=仮説（独立根拠 3 件未満）の主張。次回抽出で根拠を追加収集する候補 -->

## 明文化済み重複（参考）

<!-- 既存の明文規約と重複した主張。暗黙知として数えない。重複先パスを必ず示す -->
