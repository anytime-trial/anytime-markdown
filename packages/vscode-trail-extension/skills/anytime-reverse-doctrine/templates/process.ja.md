---
title: "{{リポジトリ名}} 開発プロセスの実態（doctrine）"
date: "{{YYYY-MM-DD}}"
type: "spec"
lang: "ja"
author: "Claude Code v{{CLI バージョン}}"
skill: "anytime-reverse-doctrine"
category: "doctrine"
excerpt: "{{200 文字以内。明文プロセスと実態の差分の要旨}}"
extractedAt: "{{ISO 8601}}"
commitRange: "{{from}}..{{HEAD}}"
sources:
    - "{{使用した入力ソース（例: git log / sessions / 明文プロセス文書パス）}}"
---

# {{リポジトリ名}} 開発プロセスの実態

## 概要

{{明文プロセスと実態の一致度・主要な差分を 2〜3 文で要約する}}

## 守られている

<!-- 明文プロセスどおりに実態が動いている項目。明文側の出典と実態側の証拠を対で示す。記入例: -->

### コミットは Conventional Commits 形式で細分化されている

- 確度: 原則
- 状態: 明文化済み重複
- 承認: draft
- 主張: コミット件名は明文規約どおり `type(scope):` 形式で付与され、実態と乖離がない。
- 根拠:
    - 「{{明文規約の逐語引用}}」 — `{{規約パス}}`
    - 「{{直近 N 件中 M 件が形式準拠（実測値）}}」 — {{集計対象の commitRange}}

## 形骸化

<!-- 明文には在るが実態が伴っていない項目。批判でなく観測事実として書く -->

## 暗黙で追加

<!-- 明文に無いが実態として一貫して行われている手順・慣行。本文書の主対象（状態=暗黙） -->
