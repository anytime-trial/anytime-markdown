// agent-core: multi-LLM agent state management
// - ProviderRegistry: backend registration and active provider selection
// - HealthMonitor: per-backend availability state
// - UsageTracker: call count and token usage aggregation
// - AgentChatSession: conversation history and model preference
// - ModelRoutingPolicy: per-task default model selection
// - mapping: agent session x worktree classification (relocated from trail-core)
export {};
